describe('suite', () => {
  it('first case', () => {
    const value = compute(input);
    expect(value).toBe(1);
  });

  it('second case', () => {
    const value = compute(input);
    expect(value).toBe(1);
  });
});
